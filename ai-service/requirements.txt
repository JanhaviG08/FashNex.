fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1
numpy==1.26.4
scikit-learn==1.5.0

# Optional — remove if TF install is problematic (service falls back gracefully)
tensorflow==2.16.1
# tensorflow-cpu==2.16.1   # lighter alternative

# For existing Flask-based analyze endpoint (keep if you use it)
# flask==3.0.3
# flask-cors==4.0.1
# Pillow==10.4.0